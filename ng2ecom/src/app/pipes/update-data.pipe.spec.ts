import { UpdateDataPipe } from './update-data.pipe';

describe('UpdateDataPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateDataPipe();
    expect(pipe).toBeTruthy();
  });
});
